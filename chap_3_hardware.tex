\chapter{Hardware}
\label{chapter:headset}


\section{Hardware Architecture and the Beamsplitter Solution}
\label{sec:hw-architecture}


\subsection{Clinical Biomarkers and Hardware Requirements}
\label{sec:hw-biomarkers-requirements}

The oculomotor assessment framework measures three biomarkers: \ab{plr} amplitude and velocity, smooth pursuit accuracy, and vergence precision. Each imposes distinct hardware demands. \ab{plr} measurement requires two distinct illumination paths: a visible stimulus source to elicit the reflex, and a separate near-infrared (NIR) imaging source that captures pupil dynamics without stimulating photoreceptors. Ambient visible light interferes with \ab{plr} measurement by reducing our control of the total stimulus intensity at the retina, while stray light reduces pupil-image contrast on the NIR sensor. Both are eliminated by enclosing the optical path in a light-sealed housing. Smooth pursuit and vergence testing impose a further constraint: the geometric relationship between the display, camera, and eye must remain constant throughout each test. A per-session calibration routine establishes the eye-to-display mapping at the start of each use, but this calibration is only valid while the device remains stationary relative to the head. A handheld device cannot guarantee this: hand tremor and postural drift shift the stimulus position frame-to-frame, contaminating the gaze signal with device motion. A rigid head-mounted structure eliminates the problem because the optics move with the user's head and the calibrated geometry is preserved throughout testing. Binocular vergence testing requires independent control of stimulus presented to each eye, demanding optical separation. Finally, screening in non-clinical settings---school athletic departments, urgent care clinics, sideline assessment---requires the device to be compact, low-cost, and operable by non-medical personnel.

These four requirements drive the hardware specification:
\begin{enumerate}
    \item \textbf{Controlled illumination:} an enclosed optical path that excludes ambient light, with a visible stimulus source ($\sim$480\,nm, melanopsin-activating) and a separate NIR imaging source (850\,nm, invisible to photoreceptors) that does not itself elicit a pupillary response.
    \item \textbf{Fixed geometry:} rigid mechanical structure; absolute positioning repeatability $\leq 1$\,mm across 6 axes.
    \item \textbf{Binocular disparity capability:} independent left/right stimulus paths.
    \item \textbf{Compact, low-cost, non-specialist operation:} head-mounted form factor consistent with consumer wearable headsets, target unit cost $<\pounds 500$, intuitive interface.\footnote{The \pounds 500 ceiling and consumer-headset form-factor target reflect the accessibility gap identified in §1.2: existing quantitative pupillometers cost thousands of pounds and are confined to clinical settings (§7.1 benchmarks competing oculomotor systems at \pounds 4{,}000--30{,}000). Both are calibrated against hardware that has demonstrably penetrated school deployments at scale---Meta Quest 3 and ClassVR education headsets at \$400--630 per unit~\cite{metaedu, classvr}, and DfE-procured school AEDs at \pounds 435 + VAT~\cite{dfeaed}.}
\end{enumerate}

\subsection{Architecture Evaluation: Pugh Decision Matrix}
\label{sec:hw-pugh-matrix}

We evaluated five candidate architectures against these four requirements (\todo{FIGURE: Pugh decision matrix described below}).

\textbf{Handheld smartphone} (handheld smartphone with custom app): Requires the user to hold the device steady while performing oculomotor tasks---an impossible ergonomic demand. Ambient light contaminates the \ab{plr} signal. Stimulus presentation is uncontrolled: no mechanical fixture ensures identical geometry between sessions, and a single screen cannot deliver independent stimulus paths to each eye. This architecture fails requirements 1, 2, and 3.

\textbf{Headset, direct viewing (no optics):} A headset mounting a display at fixed distance eliminates positional variability and enables active illumination control. However, comfortable sustained viewing of an unmagnified display at the human near point of accommodation---25--30\,cm in adults~\cite{charman2008eye}---places the display 25--30\,cm from the eye, an order of magnitude beyond the front-of-face envelope of any consumer head-mounted display. The front-mounted mass required to drive a display at this distance creates neck strain that is poorly tolerated by paediatric users in particular. This fails requirement 4: optics are required to fold the optical path into a compact, wearable enclosure.

\textbf{Lensed headset, no beamsplitter:} A converging lens between display and eye creates a virtual image at far distance, allowing a compact form factor---a 50--60\,mm focal length lens places a 1\,inch display at virtual infinity for a 25--30\,mm physical position~\cite{hecht2017optics}. The lens, however, magnifies whatever sits in the display plane into a large fraction of the user's apparent visual field, and camera placement has no acceptable solution within this architecture. Placed \emph{on the display side} of the lens (off-axis, looking through the lens at the eye), the camera occupies optical territory that the lens magnifies and projects into the user's view as a visible obstruction in the virtual image. For the vergence test this is structurally fatal: the test requires fixation on a target whose apparent depth varies, and a stationary object sharing the virtual image plane introduces a fixed, conflicting depth cue, producing diplopia or fixation breaks at precisely the moments the biomarker is sampled. It also reduces the effective display field of view, eroding the immersive view that motivates the lens in the first place. Placed \emph{on the eye side} of the lens, the camera avoids the display path but inherits the constraints of the next architecture.

\textbf{Multi-camera near-eye array:} Apple Vision Pro and several Meta headsets resolve the lensed-headset placement problem by mounting small camera modules in the eye-relief space between the lens and the eye, off-axis to the user's line of sight. The resulting steep imaging angle is compensated with multiple imaging modalities fused across several cameras per eye---multi-wavelength NIR imaging and multiple Purkinje-reflection glints~\cite{berkner2021eyetracking}. Despite this investment, Apple does not expose pupil diameter to applications, including for explicit medical-research requests~\cite{huang2024avpaccuracy}, suggesting that even multi-camera multi-glint near-eye arrays do not deliver clinical-grade absolute pupillometry---they target gaze, where the pupil--glint differential structure cancels much of the geometric distortion. The architecture is also infeasible from a cost perspective because each eye would require multiple miniature cameras synchronised with patterned illuminator arrays. The form-factor requirement compounds the cost: cameras small enough to fit in 15--20\,mm of eye relief without obstructing the display fall in a sensor segment with poor low-light NIR sensitivity, further pushing toward higher-grade (and higher-cost) parts. This architecture fails requirement 4.

\textbf{Beamsplitter headset (proposed):} A partially reflective optical element at $45^{\circ}$ to the display axis reflects the display image toward the user's eyes while transmitting the light from the eye back through the element to a camera positioned behind it. This architecture decouples the camera viewing path from the user's viewing path geometrically: a single camera per eye sits coaxial with the user's line of sight, neither obstructing the display nor occupying the magnified virtual-image plane, and without the multi-camera-and-glint compensation that off-axis near-eye placement demands. NIR imaging illumination enters coaxially with the camera, while visible stimulus light is delivered via the display path---satisfying decoupled illumination control (requirement 1). The optical assembly is rigidly mounted within a compact enclosure (requirement 2). Binocular testing is enabled by presenting independent stimuli to each eye via optical splitting (requirement 3). The single-camera-per-eye design keeps unit cost within the \pounds 500 envelope (requirement 4). All four requirements are simultaneously satisfied.

\todo{FIGURE: Pugh decision matrix (table) evaluating five architectures against four requirements. Rows: handheld smartphone, headset no optics, lensed headset no beamsplitter, multi-camera near-eye array, beamsplitter headset. Columns: controlled illumination, fixed geometry, binocular disparity, compact/low-cost. Marks: ``fails'', ``partial/compromised'', ``satisfies''. Multi-camera near-eye array satisfies illumination, fixed geometry, and binocular disparity but fails on cost. Beamsplitter row is all ``satisfies''. This is the only architecture satisfying all requirements.}


\subsection{The Beamsplitter Principle}
\label{sec:hw-beamsplitter-principle}

A partially reflective surface (beamsplitter) oriented at $45^{\circ}$ to both the display axis and the camera axis creates an elegant geometric solution. Light from the display (positioned above the beamsplitter) reflects at $45^{\circ}$ toward the user's eyes. Light from the eye (incident on the beamsplitter on-axis) transmits back through the beamsplitter to the camera positioned directly behind it. The selected design uses a \textbf{longpass dichroic plate beamsplitter} at $45^{\circ}$ AOI, routing visible light (display $\to$ eye) by reflection and NIR light (eye $\to$ camera) by spectral transmission rather than amplitude splitting; this delivers $\sim$1.7--1.9$\times$ more flux on each critical path than a neutral 50:50 split (full spec in \Cref{sec:hw-beamsplitter-spec}).

Between the beamsplitter and the eye, a plano-convex BK7 converging lens is positioned (focal length and lens-architecture rationale in \Cref{sec:hw-optical-design}). This lens serves two purposes: it creates a virtual image of the display at a distance beyond the near point of accommodation (eliminating vergence strain), and it collimates the light from the eye, improving camera focus tolerance and reducing field-of-view limitations~\cite{kowalski2021hybrid, hecht2017optics}. The camera itself is positioned at a conjugate image plane behind the beamsplitter, set to focus on the eye's pupil and iris.

This arrangement---display above beamsplitter, lens between beamsplitter and eye, camera behind beamsplitter on-axis---resolves the fundamental conflict between user viewing comfort (display at fixed, ergonomic depth) and imaging geometry (camera on-axis for minimal foreshortening). The beamsplitter makes the design possible; all downstream optical, mechanical, and electronic subsystems are constrained by this geometric choice.

\todo{FIGURE: Optical path diagram. Side view schematic showing: display panel (top), $45^{\circ}$ longpass dichroic beamsplitter (center), plano-convex lens (between beamsplitter and eye, curved face toward display), eye (right), camera (bottom, behind beamsplitter). Annotate: (1) display light path ($\nearrow$ at $45^{\circ}$ to eye, visible reflected), (2) eye image path ($\swarrow$ NIR transmitted through beamsplitter to camera), (3) active illumination path (downward, coaxial with camera), (4) focal length $f$, (5) beamsplitter angle, (6) working distance from lens to eye ($\sim$40\,mm).}


\subsection{Downstream Requirements}
\label{sec:hw-downstream-requirements}

The choice of beamsplitter defines the optical design space for \Cref{sec:hw-optical-design}. The beamsplitter's position and reflectivity determine the required lens focal length and numerical aperture. The need for coaxial illumination and the spectral sensitivity of melanopsin (peak $\sim$480\,nm) constrain the light source wavelength and intensity. The binocular testing requirement necessitates either dual beamsplitters (one per eye) or a more complex folding system to present independent stimuli. These constraints---focal length, beamsplitter spec, illumination wavelength, binocular optical split---are addressed systematically in the lens design and illumination strategy that follows.


\section{Optical Design: Lens System, Beamsplitter, and Illumination Strategy}
\label{sec:hw-optical-design}

Without magnification, comfortable close-range viewing of a smartphone display requires the eye to accommodate to approximately 25--30\,cm, limiting visual comfort during extended use and imposing bulky design constraints. In \ab{mtbi} populations, accommodation dysfunction may further degrade comfort and measurement fidelity. A relay lens system addresses this constraint by creating a virtual image at infinity or comfortable viewing distance, allowing the eye to relax and the display to be positioned deeper within the headset enclosure. This section justifies the selection of each optical component through first-principles analysis, empirical focal length trade-offs, and quantitative comparison of candidate illumination wavelengths. The design trades component cost (all $<\pounds 20$ per unit) against positioning tolerance, aberration control, and measurement fidelity.


\subsection{Lens Architecture Selection}
\label{sec:hw-lens-architecture}

The relay lens performs double duty under the dichroic architecture: it relays the visible display image to the eye AND must transmit cleanly across both visible (400--700\,nm reflected to eye) and 850\,nm NIR (transmitted from eye to camera through the same lens). This co-use changes the candidate set from ``comfortable virtual image'' alone to ``comfortable virtual image AND clean pupil-edge image at the camera.''

We adopt \textbf{pupil-edge image quality at the camera} as the single discriminator. Pupil centroid error scales sub-linearly with edge sharpness, but pupil \textit{boundary} segmentation---required for sub-mm pupil diameter measurement (the diagnostic metric)---degrades fast once the edge transitions across more than $\approx 2$ sensor pixels. Anything that smears the pupil edge at 850\,nm directly attacks measurement fidelity, which subordinates other axes (cost, mass, axial form factor) so long as those remain within mobile-headset bounds.

Five candidates were evaluated against this discriminator: plano-convex, biconvex, aspheric singlet, Fresnel, and visible+NIR achromatic doublet. \textbf{Fresnel is rejected outright}: its concentric grooves act as a diffraction grating across both bands, producing structured stray light and edge halation in the camera image---a known failure mode for any pupil-segmentation pipeline and not recoverable in software. \textbf{Achromat is rejected as overkill}: the visible/NIR axial chromatic shift across a single BK7 singlet at $f = 100$\,mm is sub-mm ($\Delta f \approx 0.6$\,mm between $\lambda = 550$\,nm and $\lambda = 850$\,nm), small enough to be absorbed by a fixed mechanical offset between the display plane and the camera sensor plane. A doublet would eliminate the shift but adds \pounds 15--30 per unit and ${\sim}3\times$ mass for a benefit already absorbed mechanically. \textbf{Plano-convex is marginally preferred} to biconvex at our f-number ($\approx f/3.3$): for an on-axis source it minimises spherical aberration when the curved surface faces the collimated side, giving a tighter pupil-edge PSF. The difference is small (a few \% in encircled-energy radius) but free.

The \ab{mvp} swept singlet focal lengths empirically from 40 to 150\,mm with biconvex stock and converged on \textbf{$f = 100$\,mm}, which \Cref{sec:hw-virtual-image} derives from first principles via accommodation tolerance. Theory and experiment agree, and no other axis in the trade space (depth, mass, cost) shifts the ordering. The production design therefore locks \textbf{plano-convex singlet, BK7, $f = 100$\,mm}, with the curved face oriented toward the display side.

One honest caveat: the empirical 40--150\,mm sweep was conducted with biconvex stock only, before the dichroic pivot made cross-band edge sharpness the discriminator. The plano-convex preference is therefore predicted, not measured, on this rig. Final-build verification (re-imaging a known-pupil target through both singlets at 850\,nm and quantifying edge sigma) is included in the \Cref{sec:hw-vergence-specs} commissioning checklist.

\todo{FIGURE: Lens architecture trade table --- five candidates $\times$ four axes (pupil-edge PSF at 850\,nm; visible/NIR chromatic shift; cost \pounds/unit; mass g). Rows: plano-convex, biconvex, aspheric, Fresnel, achromat doublet. Cell shading indicates pass / marginal / fail against each axis.}


\subsection{Virtual Image Distance and Focal Length Tolerance}
\label{sec:hw-virtual-image}

The thin lens equation governs virtual image formation:
%
\begin{equation}
    \frac{1}{f} = \frac{1}{v} - \frac{1}{u}
\end{equation}
%
where $f$ is focal length, $u$ is object distance (display to lens), and $v$ is image distance (positive real, negative virtual). Rearranging:
%
\begin{equation}
    |v| = \frac{f \cdot u}{f - u}
\end{equation}

For comfortable viewing, the virtual image should be placed at infinity or beyond the eye's near point (typically 0.25--0.5\,m). Setting $u \approx f$ places $|v| \to \infty$ (relaxed accommodation); small deviations create finite but manageable accommodation demand.

The selected design uses a plano-convex BK7 singlet (lens-type rationale: \Cref{sec:hw-lens-architecture}) with $f = 100$\,mm positioned at $u \approx 95$\,mm. The resulting virtual image distance is:
%
\begin{equation}
    |v| = \frac{100 \times 95}{100 - 95} = 1900 \text{ mm}
\end{equation}

This corresponds to an accommodation demand of approximately 0.5 diopters, well within the relaxed viewing range for healthy adults and \ab{mtbi} populations with intact accommodation. A positioning tolerance of $\pm 2$\,mm in the mechanical enclosure shifts $u$ to 93\,mm or 97\,mm, yielding $|v|$ values of 1395\,mm and 1475\,mm respectively---accommodation shifts of $\pm 0.2$\,D. This graceful degradation is critical for a 3D-printed assembly without precision machining.

Contrast this with a shorter focal length. A $f = 50$\,mm lens at nominal $u = 48$\,mm produces $|v| = 1200$\,mm (0.83\,D). The same $\pm 2$\,mm error yields $|v| = 575$\,mm and $|v| = 1545$\,mm---accommodation shifts of $+0.9$\,D and $-0.3$\,D respectively. Some users would experience near-point focusing, reducing visual comfort. A $f = 35$\,mm lens at $u = 33$\,mm produces $|v| = 577$\,mm. A $+2$\,mm error pushes the virtual image to just 271\,mm, near the human near point and potentially uncomfortable for sustained viewing.

We evaluated focal lengths of 40, 50, 65, 80, and 100\,mm in the prototype enclosure. The analytical predictions above were confirmed through hands-on assessment of virtual image comfort and sensitivity to positioning error across the candidate lenses. These evaluations corroborate that $f = 100$\,mm delivers both comfort ($|v| \approx 1.9$\,m at nominal position) and robustness ($\pm 2$\,mm error $\to \pm 0.2$\,D shift). The trade-off is increased headset depth: 100\,mm lenses require roughly 15--20\,mm additional depth compared to 50\,mm optics. For a prototype exploring clinical measurement fidelity, this depth penalty is acceptable; future production designs could incorporate folded optics (e.g., achromatic doublets with in-plane deflection) to reduce form factor.

\todo{FIGURE: Focal length trade-off analysis. Left panel: virtual image distance $|v|$ as a function of object distance $u$ for $f \in \{40, 50, 65, 80, 100\}$\,mm. Nominal display position ($-2$ to $+2$\,mm tolerance window) shown as shaded band for each focal length. Right panel: accommodation demand (diopters) at nominal $u$ and at $\pm 2$\,mm error; colors indicate visual comfort zones (relaxed: ${<}0.5$\,D, moderate: 0.5--1.0\,D, near-point: ${>}1.5$\,D).}


\subsection{Beamsplitter Specification}
\label{sec:hw-beamsplitter-spec}

The beamsplitter is the central optical element that enables coaxial visible display delivery and NIR pupil imaging from a single shared aperture. Two routing strategies are available: amplitude splitting (a neutral 50:50 plate, dividing both bands equally between display and camera paths) or spectral splitting (a dichroic plate, routing visible and NIR by wavelength). The selected design is a \textbf{longpass dichroic plate beamsplitter} at $45^{\circ}$ AOI with a 750\,nm longpass cut: visible light (400--700\,nm) reflects toward the eye for stimulus delivery; NIR (above 750\,nm) transmits through to the camera for pupil imaging. Each band lands on its intended path by design rather than each path being halved.

The visible reflectance floor is specified at \textbf{$R_{\text{vis}} \geq 85\%$ under worst-case linear polarisation (P-pol)} averaged across 400--700\,nm, with NIR transmittance $T_{\text{NIR}} \geq 90\%$ above the cut. A neutral 50:50 plate delivers $R = T = 0.50$ in each band; the dichroic delivers $\sim$1.7--1.9$\times$ more flux on the two critical paths (display $\to$ eye and eye $\to$ camera) without cross-contamination. This gain propagates to two downstream specs. The post-dichroic transmission budget for the display path is $\tau_{\text{total}} \approx 0.88$ ($R_{\text{vis}}$ 0.90 nominal $\times$ AR-lens 0.99); combined with the projected solid angle of the virtual image at the eye ($\Omega_{\text{proj}} \approx 0.21$\,sr), the master conversion is $L_{\text{display}} \approx 5.3 \times E_{\text{cornea}}$. Anchoring the stimulus-response curve to Truong \& Ciuffreda (2016) with spatial-summation correction per Park \& McAnany (2015), this fixes the display luminance floor at $\geq 1000$\,cd/m\textsuperscript{2} for $\geq 2.5$\,mm transient \ab{plr} amplitude ($Z \geq 3$ against both acute $+35\%$ (Master 2020) and chronic $-20\%$ (Ciuffreda 2015) concussion directions at the 0.15\,mm pupil-tracking noise floor---full derivation in supplementary brightness decision trace~\todo{CONTENT GAP: derivation appendix to be added}). The NIR LED power budget relaxes by approximately the same factor versus neutral splitting, easing the IEC 62471 eye-safety margin (cross-reference Eesh's H\&S analysis in \Cref{sec:electronics}).

\textbf{Cut wavelength.} The longpass cut at 750\,nm sits one transition slope ($\sim$10--20\,nm) above the deepest red display content ($\sim$700\,nm) and $\geq 50$\,nm below the chosen 850\,nm NIR illumination line. The 50\,nm guard protects against AOI shift, manufacturing tolerance, and LED spectral spread. Catalogue cuts step at 650/700/750/800\,nm; 750\,nm is the only stocked standard that sits cleanly between the two constraints.

\textbf{Polarisation at $45^{\circ}$ AOI.} Hard-coated dielectric edges at oblique incidence are intrinsically polarisation-dependent (Macleod, \textit{Thin-Film Optical Filters} Ch.~8): S- and P-polarised components see slightly different effective edge wavelengths, with a 5--8 percentage-point S vs P in-band reflectance gap typical of catalogue parts. With unpolarised $R_{\text{avg}} \geq 90\%$ published, worst-case P-pol lands at 82--88\%; the $R_{\text{vis}} \geq 85\%$ floor sits inside this envelope as a defensible spec without requiring a premium part. No constraint on display polariser orientation is propagated to panel selection: the landscape mounting requirement (panel long edge along IPD axis) already locks panel orientation, and the brightness recompute confirms $Z \geq 3$ holds under the worst-case polarisation assumption.

\textbf{Plate dimensions and substrate.} The plate clear aperture must intercept the full eye-side optical aperture and the full display footprint at $45^{\circ}$ projection. With the locked $f = 100$\,mm plano-convex relay lens (\Cref{sec:hw-lens-architecture}) and $\pm 15^{\circ}$ worst-case eye rotation through 15\,mm eye relief, the eye-side beam diameter at the plate is $\sim$25\,mm; projected at $45^{\circ}$ this requires a clear aperture of $\sim$25 $\times$ 35\,mm (rectangular). Standard catalogue sizes (25.4 $\times$ 36.0\,mm or 50.8\,mm-diameter round) accommodate this with margin. Substrate thickness 1.0--1.6\,mm is the catalogue norm; thinner is preferred for rear-surface NIR ghost suppression, with NIR-optimised AR on the rear face suppressing the residual ghost below the camera noise floor.

\textbf{Stray light.} Under the corrected geometry (display above pointing down; eye-dichroic-camera collinear horizontal), display NIR emission and the $\sim$5--10\% residual visible that fails to reflect both transmit downward through the plate, away from the camera. A matte-black absorber on the bottom face of the optical block plus internal housing blackening suppresses indirect scatter. Standard practice; near-zero \ab{bom} impact.

\textbf{Selection band and cost.} Candidate parts include Thorlabs DMLP-series (longpass) and Semrock/Edmund TECHSPEC longpass equivalents at 750\,nm cut. Component cost at modest volumes is \pounds 25--50 per unit---a step up from the \pounds 5--10 neutral plate but a small fraction of the \pounds 200--300 device launch \ab{bom}, dominated at the system level by the brightness, LED-power, and camera-SNR gains it unlocks. Selection criteria: characterised at $45^{\circ}$ AOI (not $0^{\circ}$); NIR-optimised rear-face AR; meets $R_{\text{vis}} \geq 85\%$ / $T_{\text{NIR}} \geq 90\%$ at the specified polarisation worst case. Specific part selection is deferred to procurement-stage datasheet verification.

\todo{FIGURE: Beamsplitter spectral response and routing. Left panel: dichroic R/T spectrum vs wavelength (400--1000\,nm) with 750\,nm longpass cut annotated; visible band (400--700\,nm) shaded, 850\,nm NIR LED line marked, 50\,nm guard band shown. Right panel: optical-block schematic (display top, eye-dichroic-camera collinear horizontal) with colour-coded ray paths: visible reflected to eye, NIR transmitted to camera, residual visible/NIR transmitted downward to absorber.}


\subsection{Illumination Strategy: Visible vs Infrared}
\label{sec:hw-illumination-strategy}

All commercial clinical pupillometers use NIR illumination, not visible light. This choice reflects a fundamental constraint on \ab{plr} measurement during dark adaptation recovery. The human \ab{plr} is mediated by intrinsically photosensitive retinal ganglion cells (ipRGCs, expressing melanopsin) and classical rod and cone photoreceptors, with peak spectral sensitivity near 480\,nm (blue-green) for ipRGC contribution and 555\,nm (green) for photopic cone vision. Any visible illumination applied during dark-phase recovery stimulates these pathways, modulating pupil redilation velocity and complicating interpretation of the underlying \ab{mtbi}-related accommodation and vergence dysfunction.

NIR light (850--940\,nm) does not activate melanopsin or classical photoreceptors, decoupling the imaging source from the \ab{plr} pathway. This is the established standard in clinical pupillometry: visible-light imaging biases recovery kinetics and confounds diagnostic measurements. For \ab{mtbi} screening, where \ab{plr} dynamics are diagnostic markers, NIR illumination is mandatory.

Beyond the confound argument, NIR offers a secondary optical advantage. The pupil appears as a high-contrast dark region against the lighter iris in NIR imaging because retinal pigment epithelium (RPE) at the back of the eye absorbs strongly across 700--1100\,nm, while the iris stroma reflects diffusely. This contrast is especially pronounced in dark irides (high melanin content), where visible-light imaging struggles to discriminate the pupil boundary. NIR shifts the contrast mechanism from pigmentation (which confounds visible imaging) to scattering and absorption, yielding robust pupil segmentation.

Wavelength selection within the NIR band requires balancing four competing factors: (1) Silicon CMOS sensor quantum efficiency (QE) peaks around 850\,nm for most smartphone and machine-vision sensors, declining sharply above 900\,nm. (2) Eye safety---longer wavelengths generally pose lower thermal hazard per unit irradiance, but shorter wavelengths within the NIR band are still safe under appropriate power limits (cross-reference Eesh's IEC 62471 analysis in \Cref{sec:electronics}). (3) LED availability and cost: 850\,nm LEDs are widely stocked, low-cost ($<\pounds 1$--$2$ per unit), and exhibit high radiant efficiency (70--80\% wall-plug efficiency). 940\,nm LEDs are less common and more expensive. (4) Tissue absorption and scattering---water absorption in cornea and lens increases above 950\,nm, while melanin absorption decreases monotonically with wavelength. At 850\,nm, melanin contrast is high, transmission through the media is good, and silicon sensor response peaks. Trade-off analysis reveals 850\,nm as the optimal balance, offering the highest signal-to-noise ratio in pupil imaging while maintaining the simplest supply chain and lowest cost.

The \ab{mvp} is a two-stage illumination roadmap: an initial visible-light prototype (functional for static pupil size, vergence angle, and accommodation via far-point recovery) and a NIR production design (full \ab{plr} recovery fidelity). This staged approach validates the beamsplitter architecture and camera mounting before the more demanding optical requirement: vergence testing, which exploits the binocular display path to measure convergence insufficiency.

\todo{TABLE: Visible vs.\ NIR illumination comparison. Columns: property, visible (400--700\,nm), NIR (850\,nm). Rows: \ab{plr} confound (yes/no), pupil-iris contrast (moderate/high in dark irides), hardware cost (0/+\pounds 10 for dedicated NIR module), impact on SP/vergence measurement (confounded/independent), impact on accommodation measurement (minor/none), commercial availability (standard/needs sourcing), \ab{mvp} suitability (yes/no). Summary row: visible \ab{mvp} sufficient for static and far-point metrics; NIR production required for \ab{plr} dynamics.}


\section{Vergence Feasibility and Camera Geometry}
\label{sec:hw-vergence}


\subsection{Introduction: Clinical Justification}
\label{sec:hw-vergence-clinical}

Convergence insufficiency affects 35--49\% of \ab{mtbi} patients (versus 2--8\% baseline prevalence) and predicts a 12.3$\times$ increase in odds of prolonged recovery~\cite{mucha2014voms, master2016vision, duprey2017convergence}. The clinical gold standard---near point of convergence (\ab{npc}) testing---requires advancing a physical target toward the patient, equipment that is stationary and inaccessible in resource-limited settings. The screening headset relocates this assessment to a portable optical platform: apparent depth, controlled via interocular disparity, directly drives vergence angle. This section quantifies the constraints that enable this translation.


\subsection{Binocular Disparity and Vergence Angle}
\label{sec:hw-binocular-disparity}

The headset uses a physical septum to divide the display at the midline, enforcing that each eye observes only its respective half. Software renders a target dot---initially zero disparity (both eyes' dots aligned, depth at infinity)---then progressively increases crossed disparity to simulate target approach. As disparity grows, vergence angle increases according to the geometric relationship:
%
\begin{equation}
    \theta_c = 2 \cdot \arctan\left(\frac{\delta}{2 d_v}\right)
\end{equation}
%
where $\theta_c$ is the vergence angle (in radians), $\delta$ is the horizontal display disparity (in mm), and $d_v$ is the virtual image distance (in mm) formed by the imaging optics.

For the plano-convex relay lens specified in \Cref{sec:hw-optical-design} ($f = 100$\,mm), the virtual image distance is approximately 1900\,mm. Consider a worked example: if the software commands a display disparity of 10\,mm, the resulting vergence angle is:
%
\begin{equation}
    \theta_c = 2 \cdot \arctan\left(\frac{10}{2 \times 1900}\right) \approx 0.30^{\circ}
\end{equation}

This modest angle reflects the large conjugate distance. The clinical relevance is absolute: at this vergence demand, a patient with convergence insufficiency would fail to fuse, and the vergence range traversed (from 0 to $0.30^{\circ}$ and beyond, to the fusion limit) quantifies their deficit.

A critical design implication emerges from the equation above: \textbf{vergence angle is inversely proportional to virtual image distance.} Shorter focal length lenses produce shorter conjugate distances and thus larger vergence angles for identical display disparities. This coupling means that lens selection directly determines the vergence test range and must be coordinated with the disparity budget available on the display hardware. The 100\,mm focal length was selected to keep the virtual image distance practical (sub-2-metre) while maintaining sufficient angular resolution to distinguish healthy from pathological vergence profiles.

\todo{FIGURE: Binocular optical path schematic. Schematic cross-section (sagittal plane) showing left and right eye apertures, longpass dichroic beamsplitter, plano-convex relay lens ($f = 100$\,mm), display with septum at midline, and target dots rendered at position $\delta$ to each eye. Vergence angle $\theta_c$ subtended at the eyes is annotated.}


\subsection{Interpupillary Distance and Demographic Accommodation}
\label{sec:hw-ipd}

The target population---children and adolescents aged 8--18 years---exhibits significant anatomical variation. Interpupillary distance (IPD) ranges approximately 48--65\,mm across this cohort~\cite{maclachlan2002interpupillary}. The screening headset employs a mechanically symmetric design: paired plano-convex BK7 singlets of identical focal length, a longpass dichroic plate beamsplitter spanning both eyes, and a septum positioned at the display midline.

For patients with IPD near 56\,mm (population median), the optical axes align nearly perfectly with lens principal planes, and the vergence measurement is unbiased. Users at the extremes of the IPD range (e.g., 48\,mm or 65\,mm) experience modest asymmetry in the conjugate distance and effective focal length, slightly degrading vergence precision. However, this residual error remains clinically acceptable: the vergence signal (differences on the order of $0.5$--$1.0^{\circ}$ between healthy and pathological patients) is substantially larger than the induced bias. Accommodating variable IPD through mechanical adjustment (e.g., adjustable nose bridge) is deferred to the engineering design section and does not affect the feasibility analysis here.


\subsection{Vergence--Accommodation Conflict}
\label{sec:hw-vac-conflict}

In natural vision, convergence and accommodation vary covariantly: fixating on a near object requires both lens thickening (to focus) and convergence (to align the optical axes). The headset decouples these: binocular disparity commands vergence angle, but accommodation is fixed by the virtual image distance ($\sim$1.9\,m for the 100\,mm lens).

This introduces a fundamental measurement assumption: \textbf{the headset measures fusional vergence under fixed accommodation, not accommodative vergence.} Fusional vergence is the range of convergence that can be sustained when accommodation is held constant; it is slightly broader than the vergence range measured in clinical \ab{npc} testing, where accommodation varies with fixation distance~\cite{horwood2013clinical, schor1992dynamic}. However, the relationship is strong and monotonic: patients with convergence insufficiency show reduced fusional vergence and reduced accommodative vergence.

For brief assessment windows (30--60\,seconds per test), the mismatch induces minimal discomfort and does not confound the detection of pathology. Future validation studies must establish the correlation between headset-measured fusional vergence and clinical \ab{npc}; this is a measurement validation task, not a design failure. The explicit acknowledgment of this assumption satisfies the requirement to examine design tradeoffs critically.


\subsection{Specifications Summary}
\label{sec:hw-vergence-specs}

The analysis yields three binding specifications: the display must support $\geq 10$\,mm crossed disparity (\Cref{sec:hw-binocular-disparity}), the camera must maintain $\alpha < 5^{\circ}$ (\Cref{sec:hw-biomarkers-requirements}), and the mechanical design must accommodate IPD variation of 48--65\,mm with residual bias $< 5\%$ (\Cref{sec:hw-ipd}). These constraints propagate directly into the engineering design considerations that follow.


\section{Electronics}
\label{sec:electronics}


\subsection{System Requirements and Objectives}
\label{sec:electronics-requirements}

\subsubsection{Purpose of the Electronics Subsystems}

The purpose of the electronic subsystem within the headset is to enable precise, repeatable and time-stamped pupillometry data collection and inference, since quantitative pupil and eye-movement metrics have been reported as relevant markers of neurological dysfunction~\cite{mcdonald2022eye}. In order to meet its objectives, the subsystem must include devices which are able to (i)~present visual stimuli, (ii)~provide stable near-eye IR illumination, (iii)~capture synchronised binocular eye video, (iv)~timestamp and store recorded data points and (v)~process and run inference on recorded video.

\subsubsection{Measurement Requirements}

Having covered the purpose of the electronics subsystems, the next step is to highlight the overarching specification requirements based on the biological features of the chosen biomarkers.

In order to capture pupillary constriction dynamics, vergence response latency, and smooth pursuit tracking behaviour, which occur at short time intervals, the electronics system must support high frame-rate eye measurement of ${\sim}120$\,Hz~\cite{bergamin2003pupil}.

Identifying the vergence biomarker also requires an analysis of the coordinated behaviour of both eyes~\cite{yaramothu2019vergence}. Therefore, the design must include binocular capture with balanced near-eye IR illumination.

\subsubsection{Use-Case Driven System Requirements}

The electronics architecture must reflect the device's use case, which is that of a portable screening tool for pitch-side use in amateur sport. Therefore, the prototype must be (i)~battery-powered, (ii)~self-contained and (iii)~wearable, (iv)~intuitive enough to be operated by a user without a medical/technology background (v)~basic resistance to sweat and rain.

This introduces the following practical and safety system requirements. The system must be capable of (i)~operating locally without continuous internet connectivity, (ii)~managing connectivity interruptions, (iii)~interrupting sessions in the case of head-movement (iv)~activating guard-rail procedures to protect user in the event of illumination, battery, and overheating mishaps.

From a design criteria perspective, the project requires the device to adhere to (i)~accessibility (ii)~accuracy and (iii)~convenience.

\subsubsection{Prototype Scope}

The stages of the prototype outlined in \Cref{tab:prototype-stages} highlight a progression of subsystem validation through to pre-production readiness. Within the scope of the 3YP, the electronics design is taken to the scope of Stage~1: subsystem validation. The following sections are therefore treated as an academic engineering exercise whose purpose is to isolate and determine the viability of the subsystems described. Subsequent stages introduce dependencies on user-feedback, regulatory constraints and manufacturing factors that require iterative testing and external input; meaningful assumptions are made for these but their limitations are highlighted explicitly~\cite{blanchard2011systems}.

\begin{table}[htbp]
    \centering
    \caption{Prototype development stages for the headset electronics.}
    \label{tab:prototype-stages}
    \small
    \begin{tabular}{p{2.6cm}p{2.4cm}p{3cm}p{3cm}p{3cm}}
        \toprule
        \textbf{Stage} & \textbf{Type} & \textbf{Primary purpose} & \textbf{Evaluation focus} & \textbf{Outcome / next step} \\
        \midrule
        \textbf{Stage 1: Subsystem validation (current)} & Functional technical prototype & Validate the core electronic functionality and sensing chain. & Verify sensor operation, signal acquisition, basic processing and subsystem interfacing. & Identify and correct hardware or interface issues; establish baseline functional performance. \\
        \midrule
        \textbf{Stage 2: Integrated system prototype (MVP)} & Functional technical prototype & Validate end-to-end integration including sensing, processing and inference. & Evaluate signal quality, synchronisation, motion robustness and initial inference performance. & Refine filtering, thresholds and system parameters; confirm measurement reliability. \\
        \midrule
        \textbf{Stage 3: Pre-deployment prototype} & High-fidelity functional prototype & Validate usability and system behaviour in representative use-case conditions. & Assess usability, repeatability, operator interaction and robustness in pitch-side scenarios. & Validate system-level performance prior to productisation or extended field testing. \\
        \bottomrule
    \end{tabular}
\end{table}


\subsection{System Architecture and Data Flow}

\subsubsection{Architecture Overview}

After having outlined the system requirements in \Cref{sec:electronics-requirements}, the next step is highlighting the system-level organisation of the headset electronics. The headset is represented as a combination of multiple subsystems with distinct roles in stimulus delivery, sensing, embedded processing, communication, and power delivery/protection. This architectural view is helpful communicating the complete measurement chain before a detailed design of each subsystem is considered in later sections~\cite{blanchard2011systems}.

\subsubsection{Functional Subsystems Decomposition}

The layout and interactions of the subsystems within the headset is highlighted in \Cref{fig:electronics-subsystems}. It consists of 4 principal internal subsystems: Stimulus and Illumination; Sensing; Compute, Storage and Communications; Power and Protection. Outside the headset boundary, a Mobile App + Backend Cloud Infrastructure is shown. The system diagram distinguishes the flow of information/control interfaces (black arrow) with the flow of power (red arrow). The decomposition shown in \Cref{fig:electronics-subsystems} is a functional diagram and does not imply PCB co-location of all elements within a given subsystem. \todo{The PCB arrangement is discussed further in Section 7.2 --- update cross-reference.}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{figures/electronics_image1}
    \caption{Functional subsystems decomposition of the headset electronics. Black arrows represent information and control interfaces; red arrows represent power flow.}
    \label{fig:electronics-subsystems}
\end{figure}

The Compute, Storage and Communications Subsystem is the central coordination block of the device. It consists of an embedded computer, local session storage and Integrated Circuits required for wireless communication. The responsibilities of the subsystem include issuing control actions to the sensing and stimulus hardware, receiving test data streams, storing captured data locally, carrying out post-capture inference and handling wireless communications with the external Mobile App + Backend Cloud Infrastructure.

The Sensing Subsystem consists of electronics that record measurement data from the user, which is then sent to the central computational block. These include a binocular camera pair and an IMU (Inertial Measurement Unit). The camera provides eye-imaging capabilities used for the extraction of pupil and eye-movement features and the IMU provides head motion related information used for a session validity assessment.

The Stimulus and Illumination Subsystem contains the hardware responsible for producing adequate optical conditions for \ab{plr}, \ab{npc} and smooth pursuit tests as outlined in \Cref{sec:bg-oculomotor}. These include an internal display to present the visual stimulus to the subject as well as near-eye IR illumination to ensure reliable eye imaging. Together, these form an ideal measurement environment.

The Power and Protection Subsystem is not only responsible for supply distribution but also for housing vital monitoring and protection systems. It contains the battery source, charging paths, monitoring electronics, and protective mechanisms associated with illumination control, low-battery handling, electrical safety and fault response.

\subsubsection{Operating Modes and Data Flow}

The headset supports two operating modes: a data collection mode (non-concussed user) and a pitch side inference mode (potentially concussed user). The difference in these operating modes lies primarily in the timing, volume and method of external data transfer. The software aspect associated with these operating modes is highlighted in \Cref{chapter:software}.

Both modes start with the compute, storage and communications subsystem coordinating stimulus presentation, sensing activity and event logging during the capture window. The binocular eye video and IMU data are captured locally and written to on-device storage alongside any associated session metadata.

In the data collection mode, the complete session data is retained for future off-device use. This includes the binocular video, IMU data, event logs and timestamps and extracted features. Instead of transferring the data alongside inference, the headset stores the session data locally and then uploads it to backend cloud storage over Wi-Fi at a later time. Wi-Fi was chosen for this model due to heavy bandwidth requirement for transmission and the flexibility of being able to achieve a deferred upload once a strong Wi-Fi connection is achieved.

In the pitch side inference mode, the headset performs local inference from the captured session data. The headset then sends a compact results packet to the mobile app over Bluetooth. Bluetooth was chosen as the transmission medium for this mode due to the small data packet being transferred and the mobile app is required to be in close proximity to headset anyway. If the connection is interrupted, the headset retains the inference results locally and transmits it when the link is restored.


\subsection{Camera and I/O Systems}

\subsubsection{Role and Design of the Camera Subsystem}

\Cref{sec:electronics-requirements} highlighted that the headset must be able to support binocular eye imaging at 120\,Hz. The role of this section is to resolve the following practical questions associated with the camera subsystem and outline the well-reasoned camera module choice.

At a subsystem level, there are 4 main design choices that will be addressed in the following subsections:

\begin{enumerate}
    \item Whether the operating point should be VGA or 720p?
    \item Whether to opt for fixed focus or auto-focus?
    \item Whether to incorporate rolling shutter or global shutter?
    \item Whether to use an embedded or general purpose camera?
\end{enumerate}

Answering these questions will further help determine the bandwidth, storage and packaging requirements for the headset.

\subsubsection{Camera Subsystem Architecture}

This subsection justifies the camera-subsystem design choices introduced above.

The imaging task is not a full-face capture but reliable localisation of the pupil boundary and eye position under near-eye geometry. The required frame rate has already been established as 120\,Hz. More broadly, eye-tracking literature has justified a lower spatial resolution as a baseline when temporal fidelity is prioritised in near-eye eye tracking, as it enables higher frame rates with sufficient eye-region detail~\cite{morimoto2005eye, chugh2020eye}. Therefore, to minimise load on the processing unit, a VGA operating point can be justified over a 720p resolution.

A fixed-focus optical solution is also preferred. The headset geometry fixes the camera-to-eye spacing, with minimal variation from user-to-user. Under these conditions, auto-focus would introduce additional control complexity, power burden and software overhead with limited practical benefits. Fixed-focus optics therefore align best with the previously outlined design criteria of ease of use, accessible and robust.

The next choice concerns shutter behaviour within the camera. In a global-shutter sensor, all pixels are exposed to light simultaneously, whereas in a rolling-shutter sensor, the image is read line by line over a short time interval (\Cref{fig:shutter-comparison}). When trying to capture eye motion using a rolling-shutter sensor, intra-frame temporal skews and geometric distortion become present~\cite{farivar2016eyetracker}. Considering this, a global-shutter sensor is an appropriate choice.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.6\textwidth]{figures/electronics_image2}
    \caption{Comparison of global shutter and rolling shutter capture of a rotating fan, illustrating the geometric distortion introduced by rolling shutter readout.}
    \label{fig:shutter-comparison}
\end{figure}

The final design choice is that of the camera interface. The choice lies between an embedded camera module, a compact PCB-level imaging module, and a general purpose camera, an externally packaged device connected to the headset via USB. For a self-contained headset, the embedded approach is preferred due to its low integration risk, manageable wiring and suitable data path.

\subsubsection{Camera Module Selection}

Having established the required subsystem features, the next step is to compare practical camera modules that fit the criteria. Three candidates were shortlisted, ranked on the aforementioned design criteria and were weighted based on importance to the project.

\begin{table}[htbp]
    \centering
    \caption{Weighted camera module comparison. Project-fit scores are out of 3 (1 = poor, 3 = strong); weights reflect importance to the prototype's binocular-imaging requirement.}
    \label{tab:camera-comparison}
    \begin{tabular}{lcccc}
        \toprule
        \textbf{Criterion} & \textbf{Weight} & \textbf{Arducam OV9281} & \textbf{Pi Cam 2 NoIR} & \textbf{Pi Cam 3 NoIR} \\
        \midrule
        High-frame-rate suitability                 & 4 & 3 & 2 & 3 \\
        IR / pupillometry suitability               & 4 & 3 & 3 & 3 \\
        Shutter suitability for dynamic eye imaging & 4 & 3 & 1 & 1 \\
        Compact wearable integration                & 3 & 3 & 3 & 2 \\
        Embedded-interface suitability              & 3 & 3 & 3 & 3 \\
        Ecosystem maturity / implementation simplicity & 3 & 2 & 3 & 3 \\
        Prototype proportionality                   & 3 & 2 & 3 & 2 \\
        \midrule
        \textbf{Weighted total}                     &   & \textbf{56} & 46 & 45 \\
        \bottomrule
    \end{tabular}
\end{table}

The Arducam OV9281 is selected on weighted score. The decision is dominated by shutter type: the OV9281 is the only candidate offering a global shutter, which is essential for capturing rapid eye motion without intra-frame skew (\Cref{fig:shutter-comparison}). Its monochrome sensor is also natively well matched to 850\,nm IR illumination at the 120\,Hz frame rate established in \Cref{sec:electronics-requirements}. The Pi Camera Module variants score higher on ecosystem maturity but their rolling shutters disqualify them from pupil and saccade tracking at the temporal fidelity required.


\subsection{Illumination and Driver Electronics}

\subsubsection{Role of the Illumination Subsystem}

The illumination subsystem within the headset must be designed to satisfy 3 principal design requirements. First, it must provide sufficient iris-to-pupil contrast to support image segmentation. Second, it must minimise sensitivity to facial shadowing and headset fitting variance, allowing for repeatable measurements across sessions. Finally, it must provide uniform and symmetric illumination across both eyes, so binocular differences arise mainly from pupillary differences rather than lighting conditions.

\subsubsection{Wavelength Selection and Optical Arrangement}

The near-eye illumination must be selected such that the lighting does not interfere with the eye tracking process when the visual stimulus is presented on the internal display. To achieve this, near-infrared illumination must be used. It has been widely accepted in camera-based eye tracking as it improves pupil visibility at no cost of stimulus hindrance~\cite{hansen2010eye}. An appropriate wavelength of light must be chosen with regard to sensor sensitivity, cost and cornea exposure safety. 850\,nm was chosen as appropriate based on its prior use in eye-imaging as well as it being invisible to the eye, therefore suppressing the dazzling light effect.

The physical arrangement adopted was that of an LED illumination ring around each camera. This geometry allows for symmetric distribution of light across the eye, reduces local shadowing and helps maintain a consistent pupil appearance across the tests.

\subsubsection{Driver Topology and Electrical Implementation}

This subsection justifies the operational and driver choices associated with the illumination subsystem. The main electrical criterion is the need for a stable illumination output whilst minimising power loss and heat generation. Since LED optical output is primarily determined by drive current, and a small change in forward voltage can result in a large change in forward current, a constant-current driver is preferred~\cite{ti2007an1656}.

A specific current can be delivered to the LED via a linear current regulator or a switched buck current driver. While a linear current regulator is a simple solution, any excess voltage between the battery rail and the LED would be dissipated as heat. This is undesirable within the headset so a buck regulator becomes the more efficient choice~\cite{ti2007an1656}.

Finally, the 2 LED rings will comprise of parallel components as opposed to a single series chain. This means that a single branch failure will not disable a full ring thus allowing for the device to continue operating. This improves robustness and practical integration of the headset.

The device adopts a fixed brightness level during capture which is established by a one-time calibration prior to the data collection.

\subsubsection{Safety and Fail-Safe Behaviour}

As the illumination subsystem operates close to the eye, safety frameworks such as IEC~62471 must be incorporated~\cite{iec62471}. This evaluates the photobiological safety of light systems using exposure limits depending on intensity, viewing distance and exposure duration. Specifically, the corneal irradiance and retinal thermal hazard tests must be carried out.

For the proposed headset, the maximum continued IR exposure duration is ${\sim}30$\,s, where the lights are positioned ${\sim}7$\,cm from the eye. The corneal exposure test requires us to follow the sub-1000\,s IEC relation~\cite{osram2017iec62471}, which states the upper bound on W/sr that the LED can safely emit.

We first find the exposure limit of the eye by:
%
\begin{equation}
    E_{\text{IR,lim}} = 18000 \, t^{-3/4} = 18000 \times 30^{-3/4} \approx 1404 \; \text{W/m}^{2}
\end{equation}

From which we can find the upper bound on the power that the source sends per unit solid angle by:
%
\begin{equation}
    I_{\max} = E_{\text{IR,lim}} \times d^{2} \approx 1404 \times 0.07^{2} \approx 6.88 \; \text{W/sr}
\end{equation}

This shows that the total radiant intensity directed towards each eye must be below 6.88\,W/sr.

The headset must also satisfy the weak-stimulus retinal thermal hazard criterion as an 850\,nm IR source is used and the exposure times are greater than 10\,s.

The 850\,nm source has a wavelength hazard weighting of:
%
\begin{equation}
    R(\lambda) = 0.501
\end{equation}

Which we use comparing the weighted source radiance against the allowable limit by:
%
\begin{equation}
    L \approx \frac{I}{l \cdot b} \; R(\lambda) < \frac{6000}{\alpha} = \frac{6000}{0.011} = 545.5 \; \text{kW/m}^{2}\text{sr}
\end{equation}
%
where $l$, $b$ are dimensions of the LED and $\alpha = 0.011$\,rad.

For example, an LED with $l, b = 1$\,mm and radiant intensity $I = 0.25$\,W/sr gives a weighted source radiance of 125\,kW/m\textsuperscript{2}sr which lies below the long exposure limit of 545.5\,kW/m\textsuperscript{2}sr, indicating a safe and attainable safety requirement.


\subsection{Power System Design}

\subsubsection{Power Subsystem Requirements}

Given the project brief outlined in \Cref{sec:electronics-requirements}, the power subsystem must satisfy 4 requirements: (i)~provide main supply for the compute subsystem (ii)~distribute secondary rails for lower power peripherals (iii)~support rechargeable operation with safety considerations (iv)~remain practically sized for headset mounting.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\textwidth]{figures/electronics_image3}
    \caption{Power distribution architecture of the headset, showing the 2S LiPo battery, charging path, and split rail distribution to subsystems.}
    \label{fig:power-distribution}
\end{figure}

An obvious choice for the main power source was a LiPo battery, which is known for its versatility and use in portable electronics~\cite{tarascon2001lithium}. However, a 2-cell (2S) LiPo configuration is preferred over the single cell as its higher voltage provides more efficient conversion to the supply rails (\Cref{fig:power-distribution}). Additionally, a split rail architecture has been adopted as the headset contains components with different electrical load requirements. The compute subsystem requires a high current 5\,V rail and IMUs are better suited to a 3.3\,V rail.

\subsubsection{Power Budget and Battery Sizing}

The size of the battery is approximated by the headset's energy consumption per test then scaled to the number of tests per charge. At this stage, published figures are used for subsystem power demands instead of measured values in \Cref{tab:power-budget}. The Arducam OV9281 is the chosen camera module and the Raspberry Pi~5 is the compute platform, which are used for the power calculations.

\begin{table}[htbp]
    \centering
    \caption{Estimated power budget by subsystem and operating state.}
    \label{tab:power-budget}
    \begin{tabular}{lcl}
        \toprule
        \textbf{Subsystem / operating state} & \textbf{Representative power} & \textbf{Operating times} \\
        \midrule
        Compute subsystem during active capture/control & 6.0\,W [reference] & Pre-Capture, Capture \\
        Compute subsystem during local inference         & 9.8\,W              & Inference \\
        Two OV9281 camera modules                        & 0.4\,W [reference]  & Capture \\
        Internal display                                 & 2.0\,W [estimated]  & Capture \\
        IMU + battery telemetry                          & 0.01\,W [estimated] & Capture \\
        IR illumination subsystem                        & 2.0\,W [estimated]  & Pre-Capture, Capture \\
        \bottomrule
    \end{tabular}
\end{table}

The test for each biomarker (\ab{plr}, smooth pursuit, vergence) was timed to be 30\,s pre-test IR illumination, 30\,s active capture with stimulus and 30\,s post-capture inference.

The energy consumption per test is therefore approximated as:
%
\begin{equation}
    E_{\text{test}} = P_{\text{pre}} \, t_{\text{pre}} + P_{\text{cap}} \, t_{\text{cap}} + P_{\text{inf}} \, t_{\text{inf}}
\end{equation}
%
where
%
\begin{align}
    P_{\text{pre}} &\approx 6.0 + 2.0 = 8 \; \text{W} \notag \\
    P_{\text{cap}} &\approx 6.0 + 0.4 + 2.0 + 0.01 + 2.0 = 10.41 \; \text{W} \notag \\
    P_{\text{inf}} &\approx 9.8 \; \text{W} \notag
\end{align}

Therefore:
%
\begin{equation}
    E_{\text{test}} \approx 8.0 \times \frac{30}{3600} + 10.41 \times \frac{30}{3600} + 9.8 \times \frac{30}{3600} \approx 0.24 \; \text{Wh}
\end{equation}

The design target set was to achieve 30 tests, which amounts to about 10 users tested across all 3 biomarkers. Therefore:
%
\begin{equation}
    E_{\text{total}} \approx 30 \times 0.24 = 7.2 \; \text{Wh}
\end{equation}

Allowing for a practical margin of 25\% to account for losses, gives a design target of 10\,Wh. For a 7.4\,V 2S LiPo battery, this gives a capacity of:
%
\begin{equation}
    C \approx \frac{10}{7.4} \approx 1.35 \; \text{Ah}
\end{equation}

\subsubsection{Charging and Safety Considerations}

The principal risks associated with the battery are (i)~protected battery charging, (ii)~over-current and under-voltage protection and (iii)~controlled low-battery shutdown. To meet these, a designated charge management path is used for the 2S LiPo battery. The battery will be charged via USB-C and the device will stay turned off during charging.

The headset will also include battery-state monitoring via a fuel-gauge IC connected to the compute subsystem. This allows for battery state to be logged and a shut-down procedure to be initiated when a lower operating limit is reached.


\subsection{Control Electronics and IMU}

\subsubsection{Role of the Compute Subsystem}

The control electronics provide the central supervisory function of the headset and impose three distinct classes of requirement. The first comprises binocular video acquisition, local session storage, post-capture inference and wireless communication, which collectively demand the highest compute capability, memory and interface support, as set out in \Cref{sec:electronics-requirements}. The second comprises supervisory control: stimulus-state sequencing, IMU sampling, illumination enable and event logging. The third comprises independent low-level control, including back-up safety actions in the event of hazardous faults and hardware-triggered synchronisation. The control subsystem therefore requires direct compatibility with the sensing, power and communication subsystems, alongside sufficient timing fidelity for inference and data collection.

\subsubsection{Role of the IMU}

Although the IMU formally falls under the sensing subsystem (\Cref{sec:electronics-requirements}), its principal purpose is to validate trials based on motion detection, which makes it natural to group with the control electronics. When excessive head movement is detected by the IMU, the active session is interrupted and rejected.

\subsubsection{Control Architecture}

The control subsystem requirement could be fulfilled by two main architectural types: a single SBC (Single Board Computer) architecture, or a split architecture in which an SBC performs high-level acquisition and inference while an MCU (Microcontroller Unit) handles low-level control. The Class 1 requirement already establishes the need for an SBC, since general-purpose CPU, memory, microSD storage, Wi-Fi and Bluetooth integration are centred around it~\cite{blanchard2011systems}. The Class 2 requirements are supervisory rather than real-time, so they are already compatible with the SBC dictated by the dominant workload. The Class 3 requirements would be satisfied by introducing a separate MCU, but this would add inter-processor communication and timestamp alignment. Since the 3YP scope is restricted to Stage 1 of the prototype (\Cref{tab:prototype-stages}), the goal is to validate base subsystem functionality. The Class 3 requirements go beyond base functionality, so satisfying them does not fall within the scope of this academic exercise. The single-SBC architecture is therefore the proportionate choice for the constraints of the prototype.

\subsubsection{SBC Selection}

Three candidate SBC platforms were shortlisted for comparison: the Raspberry Pi~5, the Jetson Orin Nano Developer Kit and the Radxa ROCK 5B. The comparison criteria reflect the actual system requirements rather than headline compute performance alone. Dual-camera and wireless/storage integration were weighted most heavily as they directly constrain the feasibility of binocular capture, local-first operation and pitch-side use. Local inference capability remains important but is not the sole deciding criterion, because the baseline workflow performs inference after capture rather than within a tightly bounded real-time loop. Software maturity, power and thermal suitability and prototype practicality are also included because the platform must remain buildable, testable and supportable within 3YP scope.

\begin{table}[htbp]
    \centering
    \caption{SBC decision matrix for the supervisory compute platform. Project-fit scores: 1 = poor, 5 = strong.}
    \label{tab:sbc-comparison}
    \begin{tabular}{lcccc}
        \toprule
        \textbf{Criterion} & \textbf{Weight} & \textbf{Raspberry Pi~5} & \textbf{Jetson Orin Nano} & \textbf{ROCK 5B} \\
        \midrule
        Dual-camera integration                       & 0.25 & 5 & 4 & 4 \\
        Wireless / storage integration                & 0.20 & 5 & 3 & 4 \\
        Local inference capability                    & 0.20 & 3 & 5 & 4 \\
        Software maturity / development risk          & 0.20 & 5 & 4 & 3 \\
        Power / thermal suitability for wearable use  & 0.10 & 4 & 2 & 3 \\
        Accessibility / prototype practicality        & 0.05 & 5 & 2 & 3 \\
        \midrule
        \textbf{Weighted total}                       & 1.00 & \textbf{4.55} & 3.70 & 3.75 \\
        \bottomrule
    \end{tabular}
\end{table}

The Raspberry Pi~5 is selected. It dominates on the integration-led criteria that bound the prototype's binocular pipeline and on the software-maturity axis that determines how quickly the rest of the headset can be brought up. The Jetson Orin Nano scores highest on local inference but loses ground on wireless integration, power and thermal suitability for a wearable form factor, and on the development overhead of its CUDA-centred toolchain. The ROCK 5B is competitive on raw performance but its less mature ecosystem increases delivery risk for a Stage 1 prototype.

\subsubsection{IMU Selection}

The IMU is integrated to detect headset motion severe enough to invalidate the capture session. Head movement changes the relative geometry between the eye and the imaging system and is known to degrade eye-tracking accuracy, particularly in unconstrained or wearable settings~\cite{farivar2016eyetracker}. A 6-axis accelerometer-gyroscope device sampled at approximately 200\,Hz is sufficient because the requirement is gross-motion detection rather than full inertial tracking. This prevents corrupted measurements from entering feature extraction and inference while keeping the control logic simple, interpretable and compatible with the single-SBC architecture.


\section{Ergonomics and Assembly}

The optical and electronic subsystems above define the measurement chain in principle, but only deliver useful data if the headset is stable enough to keep the eye in the camera's working volume across the $\sim$10-min PLR protocol, comfortable enough that users tolerate that interval, and robust enough for shared pitch-side use. \Cref{fig:housing} reports the realised housing geometry, mass, and centre of mass that anchor the design choices below.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{figures/housing_figure.pdf}
    \caption{Headset housing geometry. Top, side, and front orthographic views show external dimensions in mm with the centre-of-mass position projected onto each view as a crosshair. The CoM, computed for the full CAD assembly with components in place, lies 22.4\,mm forward of the face plane: this distance is annotated on the top view, with the complementary CoM-to-rear distance shown on the side view. Lateral and vertical CoM offsets from the headset midpoint are below 0.3\,mm and are not labelled at the figure scale; the chain-dot line on the front view marks the lateral symmetry plane. The isometric render annotates the side strap anchor slots, cable grommet, and horizontal shell parting line referenced in the discussion below.}
    \label{fig:housing}
\end{figure}

\textbf{Retention and mass.} The forward-mounted optics place the assembly's CoM 22.4\,mm forward of the face plane (\Cref{fig:housing}), generating a pitching torque that, left unreacted, slides the headset down onto the cheeks and displaces the eye-box. A three-point harness, comprising two side straps and a top strap meeting at a padded occipital cradle, was selected to convert this forward torque into distributed upward load on the crown; a single rear adjuster spans the head-size range. A simple elastic band fails to react the pitch torque, and a rigid halo (as on the Apple Vision Pro) is over-engineered for sub-10-min wear and slows donning. The complete 510\,g assembly (145\,g ABS shell, $\sim$250\,g electronics, $\sim$75\,g optics, $\sim$40\,g strap and pad) is comparable to commercial VR headsets (Meta Quest~3 $\sim$515\,g; Apple Vision Pro $\sim$600\,g). Cervical joint torques scale strongly with mass ($\sim$100\% RMS increase per +750\,g of head-mounted load) and more weakly with anterior CoM displacement ($\sim$20\% per +4\,cm)~\cite{astrologo2024hmd, chihara2018workload}; the near-symmetric CAD layout, with lateral and vertical CoM offsets below 0.3\,mm, keeps the design on the mass-driven, not CoM-driven, branch of this curve.

\textbf{Face seal and ventilation.} The PLR protocol requires controlled ambient illumination (\Cref{sec:bg-oculomotor}), so the face pad must seal against stray light while distributing pressure on a curved facial geometry and tolerating cleaning between users. A PU-leather-skinned closed-cell foam pad, 8--10\,mm thick, was selected over open-cell foam (absorbs perspiration, harbours bacteria) and over medical-grade silicone (preferred for production but slow to iterate at prototype stage); the leather skin is wipeable, the foam does not absorb sweat, and a snap-fit attachment isolates cleaning from the calibrated optical bench. A fully sealed enclosure, however, traps warm humid air from breath and skin that condenses on cool optical surfaces, so baffled vents along the lower shell edge, set below the camera optical axis (\Cref{fig:housing}), admit airflow while keeping any residual stray light outside the camera's field of view. \todo{Specify cleaning agent, frequency, and pad replacement schedule.}

\textbf{Shell.} The shell must survive transport in kit bags (summer temperatures can exceed 40\,\textdegree C), drops, and cleaning, while permitting iterative geometry changes during prototyping. ABS (HDT $\sim$85\,\textdegree C, well-characterised mechanical properties under realistic FDM conditions~\cite{tymrak2014mechanical}) was selected over PLA (HDT $\sim$55\,\textdegree C, brittle on impact) and PETG (HDT $\sim$70\,\textdegree C, marginal). The shell parts along the horizontal plane visible in \Cref{fig:housing}, allowing the optical bench, electronics tray, and battery to be installed before the halves are joined; all optical components are recessed within the shell, so drops contact the shell rather than the optics. SLS-printed PA~12 is retained as the production-stage target for fatigue and abrasion resistance and ISO~10993-5/-10 certification for limited skin contact in industry-standard grades (e.g.\ EOS PA~2200)~\cite{eos2024pa2200, bazan2023pa12}, but is excluded at this stage as SLS is incompatible with bench-top iteration.

\section{Engineering Design Considerations}
\label{sec:hw-design-considerations}

The optical architecture described in \Cref{sec:hw-architecture,sec:hw-optical-design,sec:hw-vergence} was shaped throughout by health and safety, inclusivity, sustainability, and commercial constraints.

The choice of 850\,nm NIR illumination satisfies photobiological safety standards defined by IEC 62471~\cite{iec62471}; Eesh's concurrent safety analysis (\Cref{sec:electronics}) confirms that corneal irradiance remains below the 1404\,W/m\textsuperscript{2} limit for 30--60\,s exposure. This wavelength avoids the visible spectrum, eliminating glare and discomfort during test sessions. Equally important, the virtual image formed at approximately 1.9\,m (\Cref{sec:hw-virtual-image}) minimizes accommodation demand, which is critical: a device that exacerbates accommodation stress would contradict its own diagnostic purpose for symptomatic users. The brief test duration ($<60$\,s) further limits cumulative retinal exposure.

The symmetric binocular design ensures equivalent optical quality for both eyes across the IPD range (\Cref{sec:hw-ipd}), avoiding the monocular or asymmetric compromises found in some handheld variants. The head-mounted form factor removes dependence on hand steadiness---a key advantage for young, distressed, or symptomatic users who may struggle to hold a device stable.


\section{Technical Limitations}
\label{sec:hw-technical-limitations}

The headset prototype remains analytically validated but lacks empirical clinical validation against gold-standard imaging modalities (CT or MRI-confirmed mild traumatic brain injury). This represents the most significant technical boundary: the device identifies oculomotor abnormalities that correlate with \ab{mtbi} in published literature, yet we cannot claim diagnostic sensitivity or specificity without prospective cohort studies comparing headset measurements against confirmed cases. The vergence--accommodation conflict---where the eyes converge in response to binocular disparity while accommodation remains fixed at the virtual image distance ($\sim$1.9\,m)---has been characterised analytically (\Cref{sec:hw-vac-conflict}) but not validated empirically in a clinical cohort. While optical design modelling predicts acceptable image quality, the parallel development of camera selection and optical specifications prevents end-to-end image quality validation; Victor's optical design and Eesh's camera characterisation were optimised independently rather than jointly characterised in the integrated system. This decoupling means real-world gaze tracking accuracy and pupil detection robustness under field conditions (outdoor sports environments, varied lighting) remain untested.
